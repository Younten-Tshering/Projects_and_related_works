<% if user_signed_in? %>
    <% if is_admin? %>
<div class="container-fluid">
    <div class="row justify content center">
        <div class="col-md-6">

        <% if is_admin? %>
            <h3> Updated! </h3>
        <%= link_to 'Back to User management', ps4_ban_users_path, class: 'btn btn-warning' %>
        <% end %>
            
        </div>
    </div>
</div>

<% else %>
    <p><b>You Cannot Access The Page</b><p>


    <% end %>
<% end %>